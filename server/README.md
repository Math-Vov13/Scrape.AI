API

## Quick Start

create env
```sh
python -m venv .venv
```

install dependencies
```sh
pip install -r requirements.txt
```

start server
```sh
uvicorn main:app --reload
```
ou
```sh
poetry install
```